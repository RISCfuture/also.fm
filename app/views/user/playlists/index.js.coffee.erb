updateNoneVisibility = ->
    if $('[data-priority]').html().length == 0
        $('#nothing').show()
    else
        $('#nothing').hide()

$(window).ready ->
    $('[data-priority]').each ->
        new Playlist $(this), "<%= escape_javascript user_playlists_url(@user, tag: params[:tag].presence, priority: 'REPLACEME', format: 'json') %>".replace('REPLACEME', $(this).attr('data-priority')), $(this).attr('data-name'), ->
            updateNoneVisibility()
